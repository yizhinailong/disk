#!/usr/bin/env python3
# /// script
# requires-python = ">=3.10"
# dependencies = []
# ///

"""
Integration test template with PEP 723 inline metadata.

This template demonstrates the uv execution contract:
- Runs via: uv run test/integration/test_template.py
- Requires Python >=3.10
- No external dependencies (empty list)
- Exit 0 on success, non-zero on failure
"""

import sys

if __name__ == "__main__":
    print("template ok")
    sys.exit(0)
