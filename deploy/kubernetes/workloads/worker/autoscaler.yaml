apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: disk-worker
  namespace: disk
  labels:
    app.kubernetes.io/name: disk
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: disk
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: disk-worker
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: External
      external:
        metric:
          name: disk_hpa_worker_oldest_ready_age_seconds
        target:
          type: Value
          value: "30"
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 0
      selectPolicy: Max
      policies:
        - type: Pods
          value: 1
          periodSeconds: 60
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 600
      selectPolicy: Min
      policies:
        - type: Pods
          value: 1
          periodSeconds: 300
