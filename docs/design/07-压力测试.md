# 网盘系统 - 压力测试

## 1. 概述

### 1.1 压测目标

本测试旨在评估网盘系统在高并发场景下的性能表现，主要关注以下指标：

1. **吞吐量（QPS/TPS）**：系统每秒能处理的请求数量
2. **响应延迟**：请求从发出到收到响应的时间
3. **并发处理能力**：系统同时处理请求的最大能力
4. **错误率**：在高负载下系统的稳定性

### 1.2 工具选型

本系统采用 `drogon_ctl press` 作为压测工具。

### 1.3 为什么选择 drogon_ctl press

| 优势 | 说明 |
|------|------|
| 无需额外安装 | Drogon 框架内置工具，随框架提供 |
| 与框架紧密集成 | 原生支持 Drogon 应用的压测 |
| 功能足够 | 支持并发、持续时间、请求数等核心参数 |
| 输出直观 | 提供 TOTALS、TRAFFIC、TIMING、SPEED 等关键指标 |

---

## 2. 压测环境

### 2.1 前置条件

在执行压测前，确保以下条件满足：

1. 后端服务已启动并运行
2. PostgreSQL 数据库服务正常
3. Redis 服务正常
4. `drogon_ctl` 工具已安装并可用

### 2.2 服务配置

| 配置项 | 默认值 |
|--------|--------|
| 服务地址 | http://127.0.0.1:8080 |
| 压测工具 | drogon_ctl press |
| 测试账户 | admin |
| 测试密码 | Admin123 |

### 2.3 数据库状态

压测默认使用空数据库状态：

- 用户表：默认存在 admin 用户
- 文件列表：空（无文件）
- 测试场景：反映空目录下的性能表现

### 2.4 环境变量

| 变量名 | 说明 | 默认值 |
|--------|------|--------|
| BENCH_HOST | 服务地址 | http://127.0.0.1:8080 |
| BENCH_ACCOUNT | 测试账户 | admin |
| BENCH_PASSWORD | 测试密码 | Admin123 |

---

## 3. 压测接口

### 3.1 接口列表

| 接口 | 方法 | 路径 | 认证 | 速率限制 | 说明 |
|------|------|------|------|---------|------|
| 存活检查 | GET | /api/health/live | 无 | 无 | 无依赖基线性能测试 |
| 用户登录 | POST | /api/auth/login | 无 | 无 | 登录吞吐量测试 |
| 文件列表 | GET | /api/file/list | JWT | 无 | 文件浏览性能测试 |
| 上传初始化 | POST | /api/file/upload/init | JWT | 240/min/用户 | 上传性能测试（受速率限制） |

### 3.2 接口详细说明

#### 3.2.1 存活检查接口

**路径**：`GET /api/health/live`

**说明**：用于测试无依赖的进程与 HTTP 基线性能，不访问 PostgreSQL、Redis 或对象存储。`GET /api/health` 是 readiness 兼容别名，会检查依赖，不属于此基线。

**请求参数**：无

**响应示例**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "overall_status": "healthy",
    "role": "api",
    "instance_id": "disk-api-1",
    "initialized": true,
    "draining": false,
    "version": "1.0.0",
    "uptime": 42,
    "total_check_ms": 0,
    "timestamp": "2026-07-20T09:45:55Z",
    "components": {
      "runtime": {
        "status": "healthy",
        "latency_ms": 0
      }
    }
  }
}
```

#### 3.2.2 用户登录接口

**路径**：`POST /api/auth/login`

**说明**：测试用户认证的吞吐量。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| account | string | 是 | 账户名 |
| password | string | 是 | 密码 |

**请求示例**：
```json
{
  "account": "admin",
  "password": "Admin123"
}
```

**响应示例**：
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "expires_in": 7200
  }
}
```

#### 3.2.3 文件列表接口

**路径**：`GET /api/file/list`

**说明**：测试文件列表查询性能。

**认证**：需要携带有效的 JWT 令牌。

**请求头**：
```
Authorization: Bearer <access_token>
```

**查询参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| parent_id | string | 否 | 文件夹 ID，默认为根目录（0 表示根目录） |
| page | int | 否 | 页码，默认为 1 |
| page_size | int | 否 | 每页数量，默认为 20 |

#### 3.2.4 上传初始化接口

**路径**：`POST /api/file/upload/init`

**说明**：测试文件上传初始化的性能。该接口受速率限制约束（240次/分钟/用户）。

**认证**：需要携带有效的 JWT 令牌。

**请求头**：
```
Authorization: Bearer <access_token>
Content-Type: application/json
```

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| filename | string | 是 | 文件名 |
| file_size | int64 | 是 | 文件大小（字节） |
| file_hash | string | 是 | 文件内容哈希（MD5） |
| parent_id | string | 否 | 父文件夹 ID，默认为根目录 |

**请求示例**：
```json
{
  "filename": "test.txt",
  "file_size": 1024000,
  "file_hash": "098f6bcd4621d373cade4e832627b4f6",
  "parent_id": "root"
}
```

---

## 4. 脚本使用

### 4.1 目录结构

```
test/benchmark/
├── run_all.sh              # 全量压测编排脚本
├── get_token.sh            # JWT 令牌获取辅助脚本
├── bench_health.sh          # 健康检查压测脚本
├── bench_login.sh          # 登录压测脚本
├── bench_file_list.sh      # 文件列表压测脚本
├── bench_upload_init.sh    # 上传初始化压测脚本
├── bench_upload_lifecycle.py # init/chunk/complete/Range/cancel 分位延迟基线
├── bench_api_scaling.py    # 1/2/N API 无依赖水平扩展基线
├── bench_storage_workloads.py # 文件读写场景矩阵与资源证据
├── bench_s3_assembly.py   # S3 流式组装与 Worker 清理容量基线
└── requests/               # JSON 请求模板目录
    ├── health.json
    ├── login.json
    ├── file_list.json
    └── upload_init.json
```

### 4.2 运行方式

#### 4.2.1 全量压测

执行所有压测脚本的编排脚本：

```bash
bash test/benchmark/run_all.sh
```

该脚本会按顺序执行健康检查、登录、文件列表、上传初始化四个接口的压测。

#### 4.2.2 单个接口压测

执行单个接口的压测：

```bash
bash test/benchmark/bench_health.sh -n 10000 -c 100 -t 4
bash test/benchmark/bench_login.sh -n 10000 -c 100 -t 4
bash test/benchmark/bench_file_list.sh -n 10000 -c 100 -t 4
bash test/benchmark/bench_upload_init.sh -n 200 -c 10 -t 2
uv run test/benchmark/bench_upload_lifecycle.py --iterations 10 --payload-bytes 1048576
uv run test/benchmark/bench_storage_workloads.py --output /tmp/disk-storage-workloads.json
```

`get_token.sh` 必须从统一认证响应的 `data.access_token` 读取 JWT，不支持已废弃的 camelCase `data.accessToken`。生命周期脚本使用唯一随机内容避免秒传污染阶段延迟，校验 Range 字节，并记录批量软删除、回收站定位和永久删除耗时后精确清理自身创建的记录。

### 4.3 参数说明

| 参数 | 说明 | 默认值 | 备注 |
|------|------|--------|------|
| -n | 总请求数 | 10000（上传初始化: 200） | 压测发送的请求总数 |
| -c | 并发连接数 | 100（上传初始化: 10） | 同时建立的连接数 |
| -t | 线程数 | 4（上传初始化: 2） | 压测使用的线程数 |
| BENCH_HOST | 服务地址 | http://127.0.0.1:8080 | 压测目标服务器地址 |

### 4.4 请求模板格式

JSON 请求模板格式：

```json
{
  "method": "POST",
  "header": {
    "Content-Type": "application/json",
    "Authorization": "Bearer <token>"
  },
  "body": {
    "field1": "value1",
    "field2": "value2"
  }
}
```

---

## 5. 压测策略

### 5.1 分阶段方法

采用渐进式压测策略，逐步增加负载以确定系统性能边界：

| 阶段 | 总请求数 | 并发连接数 | 线程数 | 说明 |
|------|----------|-----------|--------|------|
| 基线 | 1000 | 10 | 2 | 验证服务正常运行 |
| 中等 | 10000 | 50 | 4 | 模拟常规负载 |
| 高并发 | 50000 | 200 | 8 | 模拟峰值场景 |
| 极限 | 100000 | 500 | 16 | 寻找系统性能边界 |

### 5.2 压测执行流程

1. **准备阶段**：启动服务，确认环境变量配置正确
2. **基线测试**：使用低并发验证系统可用性
3. **负载测试**：逐步增加并发，观察系统表现
4. **结果记录**：记录各阶段的性能指标
5. **瓶颈分析**：分析结果定位性能瓶颈

### 5.3 测试顺序建议

建议按以下顺序执行压测：

1. 健康检查（无外部依赖，最快定位问题）
2. 登录（验证认证性能）
3. 文件列表（验证数据库查询性能）
4. 上传初始化（验证业务逻辑和速率限制）

---

## 6. 结果分析

### 6.1 输出解读

`drogon_ctl press` 的输出包含四个部分：

#### TOTALS（总体统计）
| 字段 | 说明 |
|------|------|
| Total requests | 总请求数 |
| Requests/sec | 每秒请求数（QPS） |
| Duration | 测试持续时间 |
| Failed requests | 失败请求数 |

#### TRAFFIC（流量统计）
| 字段 | 说明 |
|------|------|
| Received | 收到的数据量 |
| Sent | 发送的数据量 |

#### TIMING（延迟统计）
| 字段 | 说明 |
|------|------|
| Avg req time | 平均请求时间 |
| Min req time | 最小请求时间 |
| Max req time | 最大请求时间 |

#### SPEED（速度统计）
| 字段 | 说明 |
|------|------|
| Transfer/sec | 传输速度（字节/秒） |

### 6.2 关键指标

| 指标 | 说明 | 健康阈值 |
|------|------|----------|
| QPS | 每秒处理的请求数 | 越高越好 |
| 平均延迟 | 请求的平均响应时间 | < 100ms |
| 错误率 | 失败请求占总请求的比例 | < 0.1% |

### 6.3 瓶颈排查路径

| 现象 | 可能原因 | 排查方向 |
|------|----------|----------|
| QPS 上不去 | CPU 瓶颈、线程数不足、连接池耗尽 | 检查 CPU 使用率、线程配置、连接池大小 |
| P99 延迟高 | 数据库慢查询、Redis 超时、IO 阻塞 | 分析慢查询日志、检查 Redis 连接、分析 IO 使用率 |
| 错误率升高 | 连接池耗尽、请求超时、资源不足 | 检查连接池配置、调整超时时间、监控资源使用 |

---

## 7. 局限性说明

### 7.1 延迟统计局限

`drogon_ctl press` 仅提供平均延迟，无法获取延迟分布：

- 无 P50/P95/P99 分位值
- 无法判断延迟抖动情况
- 如需更详细的延迟分析，可通过多次运行并记录结果来估算延迟分布

### 7.2 链路测试局限

该工具不支持多步骤链路测试：

- 无法模拟「登录 -> 浏览 -> 上传」的完整流程
- 每个接口独立压测，无法反映真实用户操作路径
- 建议手动串联多个接口进行链路压测

### 7.3 空数据库影响

压测在空数据库状态下进行：

- 文件列表查询反映的是空目录性能
- 无法反映有大量文件时的查询性能
- 分页、排序等操作在有数据时表现可能不同

### 7.4 单用户测试局限

所有请求共用同一个 admin 账户：

- 受单用户速率限制约束（上传初始化 240 次/分钟）
- 无法模拟多用户并发场景
- 认证服务的实际并发压力被低估

### 7.5 静态请求体局限

每次请求发送相同的请求体：

- 无动态参数变化
- 无法模拟真实用户的数据多样性
- 缓存命中等场景无法真实反映

---

## 8. 附录

### 8.1 JSON 请求模板

#### health.json
```json
{
  "method": "GET",
  "header": {},
  "body": {}
}
```

#### login.json
```json
{
  "method": "POST",
  "header": {
    "Content-Type": "application/json"
  },
  "body": {
    "account": "admin",
    "password": "Admin123"
  }
}
```

#### file_list.json
```json
{
  "method": "GET",
  "header": {
    "Authorization": "Bearer <access_token>"
  },
  "body": {}
}
```

#### upload_init.json
```json
{
  "method": "POST",
  "header": {
    "Content-Type": "application/json",
    "Authorization": "Bearer <token>"
  },
  "body": {
    "filename": "benchmark_test.txt",
    "file_size": 1048576,
    "file_hash": "098f6bcd4621d373cade4e832627b4f6",
    "parent_id": "root"
  }
}
```

### 8.2 drogon_ctl press 参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| -n num | 总请求数 | 1 |
| -c num | 并发连接数 | 1 |
| -t num | 线程数 | 1 |
| -f file | JSON 请求文件路径 | - |
| -k | 跳过 SSL 证书验证 | false |
| -q | 静默模式，不输出详细信息 | false |

### 8.3 压测结果记录模板

```markdown
## 压测结果记录

**测试时间**：
**测试环境**：
**测试人员**：

### 健康检查压测

| 指标 | 值 |
|------|---|
| 总请求数 | 10000 |
| QPS | xxx |
| 平均延迟 | xx ms |
| 错误率 | x.x% |

### 登录压测

| 指标 | 值 |
|------|---|
| 总请求数 | 10000 |
| QPS | xxx |
| 平均延迟 | xx ms |
| 错误率 | x.x% |

### 文件列表压测

| 指标 | 值 |
|------|---|
| 总请求数 | 10000 |
| QPS | xxx |
| 平均延迟 | xx ms |
| 错误率 | x.x% |

### 上传初始化压测

| 指标 | 值 |
|------|---|
| 总请求数 | 200 |
| QPS | xxx |
| 平均延迟 | xx ms |
| 错误率 | x.x% |

### 结论与建议

...
```

---

## 9. 分布式容量与故障压测

### 9.1 参考环境与目标 SLO

分布式压测必须记录 API/Worker 副本数、CPU/内存限制、PostgreSQL/Redis 规格、MinIO/S3 网络带宽、对象存储类型、数据集规模和分片大小。没有这些环境标签的结果只能用于本机回归，不能作为容量结论。

第一阶段生产目标如下；业务校验失败、客户端主动取消和限流拒绝不计入服务端错误率，但必须单独计数：

| 目标 | 验收口径 |
|------|----------|
| API 可用性 | 月度不低于 99.9%，readiness 主动摘除时间计入不可用窗口 |
| 上传成功率 | 共享依赖正常时不低于 99.9%，按完成的 upload task 统计 |
| 元数据 API 延迟 | 参考稳态负载下 P95 <= 200 ms，P99 <= 500 ms |
| 5 MiB 分片延迟 | 参考内网环境下 P95 <= 2 s，P99 <= 5 s |
| 上传恢复时间 | API 故障后客户端下一次重试可继续；Worker/完成租约接管 RTO <= 180 s |
| 孤儿对象收敛 | staging/final 孤儿在 24 h 内删除或进入可见 dead letter 告警 |
| 水平扩展效率 | 1 到 2 个 API 副本时，非依赖瓶颈区间吞吐增幅 >= 70% |

### 9.2 必测场景

| 场景 | 负载与故障 | 必须记录和断言 |
|------|------------|----------------|
| 单实例基线 | 1 API + 1 Worker，逐步增加并发 | QPS、P50/P95/P99、错误率、依赖资源、网络带宽 |
| 水平扩展 | 相同依赖规格下 1/2/N API | 扩展效率、负载分布、数据库连接增长、S3 吞吐瓶颈 |
| 随机路由上传 | 每个分片、complete、cancel 随机命中不同 API | 成功率、重复写比例、409 重试次数、无 sticky 依赖 |
| 重复完成争用 | 同一任务 10/50/100 并发 complete | 单一文件/配额/ref_count、CAS 失败率、P99、租约等待 |
| Worker 积压 | 暂停 Worker 产生清理/对账任务后恢复 1/2/N Worker | backlog、oldest job age、认领吞吐、重试/死信、排空时间 |
| API 故障 | 稳态上传中随机终止一个 API | HTTP 错误窗口、客户端恢复时间、上传最终结果 |
| Worker 故障 | 执行任务或完成租约期间终止 owner | 租约接管时间、重复外部调用、最终状态与对象数量 |
| 依赖抖动 | 分别注入 PostgreSQL、Redis、S3 延迟/超时 | fail-closed 行为、readiness、错误预算、恢复后积压 |
| 大文件带宽 | 多用户并发上传/Range 下载不同大小对象 | API/对象存储双向带宽、内存峰值、流式背压、超时率 |

### 9.3 指标与结果格式

`drogon_ctl press` 不提供分位延迟、多步骤上传或故障注入，因此分布式场景使用专用 Python/HTTP 压测脚本或支持场景编排的工具；现有脚本保留用于接口冒烟。每轮至少采集：

- 客户端：请求数、状态码/领域错误、P50/P95/P99/max、重试次数、上传任务成功率和实际字节吞吐。
- API：按 route/instance 的请求量和延迟、409/429/5xx、数据库/Redis/S3 调用耗时、活跃连接和事件循环负载。
- Worker：claim/success/retry/dead-letter、队列深度、oldest job age、租约接管、任务执行时长和并发数。
- 依赖：PostgreSQL 连接/锁等待/慢查询、Redis 命令量与错误、S3 请求/错误/首字节延迟/带宽、staging 与 final 对象数量和字节。

结果目录按日期和 Git commit 命名，并保存去敏配置、场景参数、原始机器可读结果和汇总。基线未实测前不得填写示例中的 `xxx`，必须明确标记 `not measured`；故障结束后还要等待租约和清理窗口，再对账数据库、配额、任务表与对象存储。

### 9.4 2026-07-19 单实例重构前基线

该结果用于重构前后本机回归，不代表生产容量。环境为 Linux 7.1.3 x86_64、AMD Ryzen 7 5800U（8 核/16 线程）、19 GiB 内存，Clang debug 构建；Disk、PostgreSQL、Redis 和 local 文件存储均在同一主机，HTTP 使用 loopback。Git 基线为 `c4b9c57`，该提交及其前两个提交只改文档，后端二进制与 1.8 节完整测试基线一致。

健康检查使用 `drogon_ctl press -n 10000 -c 100 -t 4`：10,000/10,000 成功、0 失败、2.005 s、4,988 RPS、平均 19.951 ms。该工具不输出分位值，因此不能把平均值当作 P95/P99。

上传生命周期使用 `bench_upload_lifecycle.py` 串行执行 10 轮唯一随机 1 MiB 单分片，并对每轮执行 1 KiB Range 下载和一个独立上传取消：

| 阶段 | count | min ms | P50 ms | P95/P99/max ms | mean ms |
|------|------:|-------:|-------:|---------------:|--------:|
| upload init | 10 | 4.819 | 5.144 | 6.813 | 5.417 |
| 1 MiB chunk | 10 | 11.850 | 12.812 | 14.427 | 13.127 |
| complete | 10 | 29.552 | 30.661 | 46.061 | 32.594 |
| 1 KiB Range | 10 | 43.267 | 43.772 | 44.941 | 43.907 |
| cancel | 10 | 5.673 | 5.914 | 7.781 | 6.288 |

10 个完成文件的精确清理耗时：批量软删除 3.570 ms、回收站定位 2.436 ms、永久删除 29.280 ms，总计 35.286 ms，即 283.399 files/s。脚本结束后对账 `files`、进行中 `upload_tasks`、`trash` 和 `build/temp_uploads` 中本次前缀均为 0。

资源基线：PostgreSQL `max_connections=100`，应用池配置上限 25；空闲观测时 `disk` 数据库有 2 个连接/1 个 active，其中 active 为观测命令。Redis 应用池配置 20，观测为 21 个客户端（含 `redis-cli`）、0 blocked、0 error replies；一次最终 10 轮生命周期增加 165 条 Redis 命令。local 存储配置为 4 个普通 I/O 线程、4 个 assembly I/O 线程、4 个 Blob I/O 线程，`assembly_max_concurrent=4`；认证路径预热后进程共 63 个线程。该线程总数还包含 Drogon 事件循环、数据库/Redis 客户端和认证 CPU pool，不能仅归因于文件 I/O。

复现命令：

```bash
PATH="$PWD/build/linux-debug-clang/vcpkg_installed/x64-linux/tools/drogon:$PATH" \
  bash test/benchmark/bench_health.sh -n 10000 -c 100 -t 4

uv run test/benchmark/bench_upload_lifecycle.py \
  --iterations 10 --payload-bytes 1048576 --output /tmp/disk-upload-baseline.json
```

后续多实例对比必须使用相同主机、payload、次数和依赖配置，并补充并发场景；10 个串行样本只建立功能阶段延迟基线，不足以证明 SLO 或水平扩展效率。

### 9.5 API 水平扩展测量合同

`test/benchmark/bench_api_scaling.py` 负责启动隔离数据库和 1/2/N 个真实 API 进程，并为每个实例并发启动一个 `drogon_ctl press` 进程。第一轮只压 `GET /api/health/live`：该 liveness 路径不访问 PostgreSQL、Redis 或对象存储；`GET /api/health` 是 readiness，禁止用于这一无依赖基线。该场景用于隔离 API 进程、HTTP 编解码、loopback 和压测客户端的扩展上限，不代表元数据或文件业务容量。业务/依赖负载由后续场景单独测量。

同一轮比较必须固定后端二进制、主机、API 每实例线程/连接池、聚合请求数、聚合并发和聚合客户端线程。请求、并发与客户端线程按实例数均匀拆分，客户端直接访问每个实例端口，模拟无损且完全均匀的负载均衡；该结果不包含生产 LB 开销。每个拓扑先预热，再执行至少三次正式 trial；拓扑顺序按 round 正序/逆序交替，减少温度、频率和先后顺序偏差。

本机默认拓扑为 `1,2,4`，其中 N=4 对应 8 个物理核上的 8 个 API 事件循环线程（每实例 `threads_num=2`）。主指标定义如下：

- `S(k)`：k 副本正式 trial 的 aggregate successful RPS 中位数。
- `speedup(k) = S(k) / S(1)`。
- `efficiency(k) = speedup(k) / k`。
- `gain_1_to_2 = (S(2) / S(1) - 1) * 100%`；本机回归门槛沿用 9.1，要求增幅不低于 70%，且所有正式请求成功。

每个 trial 必须保存每实例请求/成功/失败、press 持续时间与平均延迟、聚合 wall-clock RPS、API/客户端 CPU 时间、API 进程占主机逻辑 CPU 容量比例、API RSS、主机 CPU busy、loopback 字节和 PostgreSQL 连接数。进程 CPU 包含 Drogon 辅助线程；它与 `副本数 * threads_num` 的比值只用于规模对照，可能超过 100%，不得解释为精确的事件循环利用率。结果只有在请求拆分精确、0 HTTP 失败、所有 API 进程全程存活且正式 trial 数完整时有效。瓶颈按证据保守归类：整机 CPU 接近饱和时归为共享主机 CPU；API 进程 CPU 不高而客户端 CPU 或 loopback 成为主要消耗时归为负载发生器/本机网络；无依赖负载下 API 进程 CPU 接近配置 HTTP 线程预算时归为 API 进程 CPU/事件循环；本场景不得据此宣称 PostgreSQL、Redis 或 S3 没有瓶颈。

脚本输出 schema-versioned JSON，包含 Git commit/dirty 状态、操作系统/CPU/内存、构建类型、完整参数、逐 trial 原始值、中位汇总、扩展公式结果和门槛判定。可复现命令为：

```bash
uv run test/benchmark/bench_api_scaling.py \
  --replicas 1,2,4 --trials 3 --requests 60000 \
  --concurrency 256 --client-threads 8 --api-threads 2 \
  --output /tmp/disk-api-scaling.json
```

### 9.6 API 水平扩展基线（2026-07-20）

在干净提交 `af545333774fdbaf1a94a2d49d1752a82f3090c0` 上按 9.5 的命令完成正式测量。后端为 Debug 构建，二进制 SHA-256 为 `16bf97db990bd41b03045440354b98de14053e58c82ff123d03a500ebb2af60b`；主机为 AMD Ryzen 7 5800U（8 物理核、16 逻辑 CPU、20,792,360,960 bytes 内存），内核 `7.1.3-arch2-1`。每实例固定 2 个 HTTP 线程、6 条 PostgreSQL 连接和 4 条 Redis 连接；每个拓扑执行 4,000 请求预热和 3 次各 60,000 请求的正式 trial，聚合并发 256、客户端线程 8。

| API 副本 | 正式 RPS 范围 | 中位 RPS | speedup | 效率 | 平均延迟中位数 (ms) | API CPU (核) | 客户端 CPU (核) | 主机 CPU busy | loopback MiB/s | PostgreSQL 连接 | 失败 |
|----------|---------------|----------|---------|------|----------------------|--------------|------------------|---------------|----------------|-------------------|------|
| 1 | 11,337.569-11,670.255 | 11,381.231 | 1.0000 | 100.00% | 22.377 | 1.986 | 0.354 | 15.870% | 16.400 | 6 | 0 |
| 2 | 22,328.387-22,604.811 | 22,408.205 | 1.9689 | 98.44% | 11.313 | 3.940 | 0.659 | 30.986% | 31.838 | 12 | 0 |
| 4 | 36,947.400-39,192.111 | 39,183.512 | 3.4428 | 86.07% | 6.467 | 7.830 | 1.731 | 62.594% | 55.667 | 24 | 0 |

9 个正式 trial 共 540,000 个请求全部成功。1 到 2 副本吞吐增幅为 96.89%，超过 70% 门槛；N=4 时仍有 86.07% 扩展效率。4 副本的 API 进程 CPU 为 7.830 核，相当于 8 个配置 HTTP 线程预算的 97.875%，而客户端只消耗 1.731 核、整机 CPU busy 为 62.594%；liveness 负载不调用 PostgreSQL、Redis 或对象存储。因此本场景瓶颈符合预期，位于 API 进程 CPU/HTTP 事件循环一侧，而不是压测客户端、整机 CPU 容量或外部依赖。该比值仍是进程级规模对照，不是精确的事件循环利用率。

原始 schema-versioned JSON 保存在本机忽略目录 `.sisyphus/evidence/api-scaling-2026-07-20-af54533.json`。该 Debug + loopback 结果只作为代码回归基线；不能替代生产 LB、业务路由、PostgreSQL、Redis 或 S3 容量测试。

### 9.7 文件读写场景矩阵合同

`test/benchmark/bench_storage_workloads.py` 使用唯一临时数据库、临时 local staging/final 根目录和一个真实 `process_role=api` 进程，登录隔离数据库中的管理员账户后依次执行五类负载。默认配置固定 4 个 HTTP 线程、1 MiB 分片、32 MiB 文件上限、8 个完成组装并发、6 条 PostgreSQL 连接和 4 条 Redis 连接；本场景验证 local 后端业务路径，S3/MinIO 流式组装由下一项单独测量。

| 场景 | 默认负载 | 计时边界 | 正确性断言 |
|------|----------|----------|------------|
| `small_files` | 32 个唯一 64 KiB 文件，8 并发 | 每个文件 init + 单分片 + complete 的完整逻辑操作 | 无秒传；完成后逐文件完整下载并逐字节一致 |
| `concurrent_chunks` | 一个唯一 8 MiB 文件，8 个 1 MiB 分片以 8 并发上传 | 并发分片 wall clock 与各分片请求；complete 单独计时 | 8 个分片全部成功且索引完整；完整下载 MD5 一致 |
| `large_complete` | 3 个唯一 12 MiB 文件，每个先串行写入 12 个 1 MiB 分片 | 只统计同步 complete 的组装延迟与等效 MiB/s | 每次返回唯一文件 ID 和预期 MD5；完整下载 MD5 一致 |
| `range_download` | 对 12 MiB fixture 发起 128 次 512 KiB Range，16 并发 | 全部 Range 请求 wall clock 与单请求延迟 | 每次为 HTTP 206，`Content-Range`、长度和内容均精确匹配 |
| `mixed_read_write` | 120 个确定性逻辑操作：96 次 256 KiB Range 读、24 次唯一 64 KiB 完整上传，聚合并发 16、写并发最多 8 | 聚合 wall clock，同时分别统计读/写分位值、逻辑 ops/s 和字节吞吐 | 96 次读和 24 次写全部成功且内容一致，无秒传或未完成上传 |

脚本必须在数据生成和最终内容校验之外测量负载本身，并为每个场景保存逻辑操作数、实际 HTTP 请求数、成功/失败、P50/P95/P99/max、业务字节吞吐、API/客户端 CPU、API RSS 峰值、主机 CPU busy、loopback 吞吐、存储增长和 PostgreSQL 连接数。高并发分片与 mixed 场景必须精确保留任务到请求的映射；捕获异常后输出 API 日志尾部，并在停止进程后删除临时数据库和目录。

全部场景完成后，以本次唯一文件名前缀执行数据库对账：`files` 数量/总字节与成功 fixture ledger 完全一致，唯一随机内容对应同数量 `file_contents` 且 `ref_count=1`，管理员 `storage_used` 等于文件总字节、`storage_reserved=0`，本次 `upload_tasks` 不得处于 `Uploading` 或 `Finalizing`。终态任务的 `reserved_bytes` 作为创建时快照保留，其总和必须与 fixture ledger 一致；仅 `status IN (0, 4)` 的活动预留总和必须为零。另外校验 `expires_at - created_at` 与 3600 秒测试配置一致，保证 TTL 由数据库时间生成。任一 HTTP/领域失败、字节不一致、场景计数不完整或对账失败均使基线无效。结果使用 schema-versioned JSON 保存 Git/二进制/主机/配置、场景原始样本、汇总和最终对账；默认复现命令为：

```bash
uv run test/benchmark/bench_storage_workloads.py \
  --small-files 32 --small-concurrency 8 \
  --chunk-file-mib 8 --chunk-concurrency 8 \
  --large-file-mib 12 --large-trials 3 \
  --range-requests 128 --range-concurrency 16 \
  --mixed-operations 120 --mixed-concurrency 16 --mixed-write-percent 20 \
  --output /tmp/disk-storage-workloads.json
```

### 9.8 文件读写基线（2026-07-20）

在干净提交 `84476a9187a70c0a43cbe5c5d7d49bda8810b96b` 上按 9.7 的默认命令完成正式测量。后端为 Debug 构建，二进制 SHA-256 为 `55e63341ac85b9353596938f2a787cc738179039dde870a638d239a7a2f3d3e9`；主机为 AMD Ryzen 7 5800U（8 物理核、16 逻辑 CPU、20,792,360,960 bytes 内存），内核 `7.1.3-arch2-1`。单 API 固定 4 个 HTTP 线程、6 条 PostgreSQL 连接和 4 条 Redis 连接。

| 场景 | 业务吞吐 | 主要延迟 P50/P95/P99 (ms) | API CPU (核) | 客户端 CPU (核) | API RSS 峰值 | 主机 CPU busy | loopback MiB/s | final/staging 增长 |
|------|----------|-------------------------------|----------------|--------------------|--------------|---------------|----------------|--------------------|
| 小文件 | 154.740 files/s，9.671 MiB/s | 逻辑上传 43.214/72.054/73.996 | 1.207 | 0.724 | 32,702,464 B | 19.006% | 25.443 | 2/4 MiB |
| 高并发分片 | 160.173 MiB/s | 分片 29.201/39.947/39.947；complete 253.536 | 1.073 | 0.130 | 41,484,288 B | 10.474% | 52.716 | 8/16 MiB |
| 大文件完成 | 32.286 MiB/s | complete 371.677/379.438/379.438 | 0.968 | 0.027 | 38,236,160 B | 7.444% | 0.067 | 36/36 MiB |
| Range 下载 | 319.399 ranges/s，159.700 MiB/s | 41.398/86.027/102.752 | 0.399 | 1.022 | 38,277,120 B | 11.009% | 323.084 | 0/0 MiB |
| 混合读写 | 249.265 ops/s；读 49.853、写 3.116 MiB/s | 读 41.261/82.734/100.415；写 117.053/199.747/207.681 | 0.664 | 1.017 | 38,486,016 B | 13.256% | 109.104 | 1.5/3 MiB |

5 个场景共执行 444 个负载 HTTP 请求和 60 个完整下载校验，所有逻辑操作均成功。最终对账包含 60 个文件和 60 个唯一内容，共 49,807,360 bytes；`ref_count` 全部为 1，60 个上传任务均已完成，活动任务预留和用户 `storage_reserved` 均为 0，`storage_used` 与文件总字节一致，任务 TTL 最小/最大值均为 3600 秒。

local 大文件完成期间 API 消耗 0.968 核、客户端仅 0.027 核且 loopback 几乎为零，当前 Debug 基线的主要限制位于单次 local 组装的 API CPU/存储路径。Range 和混合场景的客户端 CPU 高于 API，loopback 统计又同时累计收发字节，因此这两个吞吐值是回归基线，不是服务端容量上限。本次 API-only 拓扑不消费 staging cleanup 任务，表中 staging 增长用于记录完成前分片和组装临时对象，不代表 Worker 清理后的稳态占用。

原始 schema-versioned JSON 保存在本机忽略目录 `.sisyphus/evidence/storage-workloads-2026-07-20-84476a9.json`。该 Debug + loopback + local 结果只作为代码回归基线；S3/MinIO 流式组装、故障中施压和积压恢复必须分别测量。

### 9.9 S3 流式组装测量合同

`test/benchmark/bench_s3_assembly.py` 使用唯一临时数据库、一个 `process_role=api` 进程、一个后续启动的 `process_role=worker` 进程和唯一 S3 staging/final 前缀。未设置 `DISK_S3_ENDPOINT` 时，脚本在随机本地端口自启动 Moto S3-compatible endpoint 并创建临时 bucket；设置 endpoint、bucket 和凭据环境变量时，脚本改用外部 MinIO/S3，凭据不接受命令行参数。结果必须记录 endpoint 类型与版本/用户声明；Moto 数据只能作为协议和代码回归基线，不能代表 MinIO 或云 S3 容量。

默认工作负载是 3 个唯一 32 MiB 文件，每个按 1 MiB 分片、4 并发预先写入 S3 staging；预载不计入组装带宽。API 固定 4 个 HTTP 线程、6 条 PostgreSQL 连接、4 条 Redis 连接、16 条 S3 连接、4 个 S3 I/O 线程、1 MiB assemble read buffer 和 1 个组装并发槽位；Worker 使用 1 并发、100 ms 轮询。所有 complete 串行执行，保留每次 HTTP 延迟原始样本，并从 `/metrics` 前后差值提取 `assemble`、`promote`、`commit` 阶段的 count/sum；组装带宽以总业务字节除以 `assemble` sum 计算，不用包含 DB 和 promote 的 HTTP 总时间代替。

测量分为两个不竞争的阶段：

1. **API 组装/提升**：Worker 未启动时完成 3 个任务，记录 API、压测客户端和可管理 S3 endpoint 的 CPU seconds/等效核数/RSS 峰值、主机 CPU busy、loopback 收发总字节和 S3 依赖调用 count/sum。组装前 staging 必须是 96 个 chunk/96 MiB；组装后、清理前必须是 96 个 chunk + 3 个 assembled/192 MiB，final 是 3 个对象/96 MiB，且没有未完成 multipart。因此组装产生的 staging 临时增长为 96 MiB，组装期间 endpoint 可见总字节增长为 192 MiB。
2. **Worker 清理**：在启动 Worker 前先把 3 个 `staging_cleanup` 任务推迟，待 Worker ready 后同时释放；有界轮询至全部 `Succeeded`，记录 jobs/s、临时字节移除带宽、Worker/API/S3 endpoint CPU 与 RSS、网络和 `staging_cleanup` 指标差值。收敛后 staging 前缀必须为空，final 数量/字节不变。

最终必须流式读回每个 final 对象并校验 SHA-256，并对账 `files`、`file_contents.ref_count`、`upload_tasks`、唯一 cleanup jobs、`storage_used`、`storage_reserved` 和本地 staging 目录。任一 HTTP/领域失败、指标 count 不等于 trial 数、对象清单/字节不一致、multipart 残留、Worker DeadLetter 或数据库对账失败均使基线无效。脚本在异常时输出进程日志尾部，最终停止所有自启动进程、删除唯一 S3 前缀并销毁临时数据库。默认复现命令为：

```bash
uv run test/benchmark/bench_s3_assembly.py \
  --file-mib 32 --trials 3 --chunk-concurrency 4 \
  --output /tmp/disk-s3-assembly.json
```

对外部 MinIO 执行时使用 `DISK_S3_ENDPOINT`、`DISK_S3_BUCKET`、`DISK_S3_ACCESS_KEY`、`DISK_S3_SECRET_KEY` 和可选 `DISK_S3_REGION`；正式基线必须在报告中明确 endpoint 实现，禁止将 Moto 结果标记为 MinIO。

### 9.10 S3 流式组装容量基线（2026-07-20）

在干净提交 `7b0026a3adaa9a6c09495a5adbed2fb12e5db63e` 上按默认合同执行：Debug 二进制、AMD Ryzen 7 5800U（8 核/16 逻辑 CPU）、20792360960 bytes 主机内存、PostgreSQL 18.4、Valkey 9.1.0，S3-compatible endpoint 为本机 Moto 5.2.2。负载包含 3 个独立 32 MiB 文件，共 96 MiB 业务数据、96 个 1 MiB chunk。

| 阶段 | 业务结果 | CPU | RSS 峰值 | 网络 |
|---|---|---|---|---|
| API 组装/提升 | complete 平均 2358.893 ms；assemble 17.711 MiB/s；promote 61.067 MiB/s；端到端 13.532 MiB/s | API 3.65 CPU-s/0.515 核；Moto 3.23 CPU-s/0.455 核；客户端 0.31 CPU-s/0.044 核 | API 84,066,304 B；Moto 447,328,256 B；客户端 277,389,312 B | loopback RX+TX 405,165,356 B，54.467 MiB/s |
| Worker 清理 | 3 个任务全部 `Succeeded`，6.951 jobs/s；移除 192 MiB，444.867 MiB/s | Worker 0.07 CPU-s/0.162 核；Moto 0.14 CPU-s/0.324 核；API 0 CPU-s | Worker 36,556,800 B；Moto 284,516,352 B；API 49,680,384 B | loopback RX+TX 515,912 B，1.140 MiB/s |

3 次 complete 的 `assemble` 总时间为 5.420400 s，均值 1806.800 ms；`promote` 总时间为 1.572054 s，均值 524.018 ms。组装阶段记录 258 次 S3 依赖调用：255 次成功、3 次用于新内容去重探测的 `not_found`，失败类结果为 0。整机 CPU busy 在组装/清理阶段分别为 13.480%/9.384%，未接近整机容量。

组装前 staging 为 96 个对象/96 MiB；清理前为 96 个 chunk + 3 个 assembled/192 MiB，即 staging 临时增长 96 MiB、endpoint 可见总字节增长 192 MiB。Worker 收敛后 staging 对象为 0，final 保持 3 个对象/96 MiB，multipart 残留为 0。3 个 final 对象均完整回读并通过 SHA-256；数据库对账为 3 个文件、3 个唯一内容、`ref_count=1`、3 个已完成 S3 任务、`storage_used=100663296`、`storage_reserved=0`，API/Worker 本地 staging 均为 0。

当前实现在 API 进程的 complete 请求内同步组装和提升 S3 对象；独立 Worker 只执行持久化 `staging_cleanup` 任务，因此上表分开报告 API 组装资源和 Worker 清理资源。Moto 是同机开发服务，loopback 指标同时累加 RX 与 TX，而客户端 RSS 还包含生成测试数据的内存；上述数值只用于协议和代码回归，不作为 MinIO/云 S3 生产容量结论。原始 schema-versioned JSON 保存在本机忽略路径 `.sisyphus/evidence/s3-assembly-2026-07-20-7b0026a.json`。
