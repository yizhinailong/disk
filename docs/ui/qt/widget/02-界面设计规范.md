# Disk Qt Widget 桌面客户端 - 界面设计规范

## 1. 设计概述

### 1.1 设计原则

| 原则 | 说明 |
|------|------|
| **平台原生感** | 遵循各平台设计规范（Windows/macOS/Linux），使用 QWidget 原生控件 |
| **简洁直观** | 信息层次清晰，操作路径简短 |
| **一致性** | 统一的视觉语言和交互模式 |
| **响应式** | 窗口大小自适应，支持高分屏，使用 QLayout 进行弹性布局 |
| **可访问性** | 支持键盘导航，高对比度模式，QWidget 原生无障碍支持 |

### 1.2 目标用户特征

| 用户类型 | 特征 | 设计考量 |
|----------|------|----------|
| **普通用户** | 偶尔使用，需要引导 | 提供直观的图标和提示，简化 QToolBar 操作 |
| **办公用户** | 频繁使用，追求效率 | 支持快捷键和批量操作，优化 QTreeView 交互 |
| **专业用户** | 大量文件管理 | 提供高级搜索和过滤功能，利用 QSortFilterProxyModel |

### 1.3 技术架构概述

本客户端基于 **Qt Widgets** 技术栈构建：

| 技术组件 | 说明 |
|----------|------|
| QWidget | 所有 UI 控件的基类 |
| QStyleSheet | 类似 CSS 的样式表系统，用于统一定义外观 |
| QLayout | 布局管理器，负责控件的自动排列和大小调整 |
| QMainWindow | 主窗口框架，包含菜单栏、工具栏、状态栏 |
| QStyle | 平台原生样式，可自定义绘制 |

---

## 2. 设计系统

> 视觉样式：使用 Qt 默认 QStyle/QPalette；不在文档中定义自定义配色、字体或深浅色切换。

### 2.1 间距系统

---

### 2.3 间距系统

| 名称 | 值 | QLayout 使用 | 用途 |
|------|------|--------------|------|
| xs | 4px | `setSpacing(4)` | 图标与文字间距 |
| sm | 8px | `setSpacing(8)` | 紧凑元素间距 |
| md | 16px | `setSpacing(16)` | 标准间距 |
| lg | 24px | `setSpacing(24)` | 区块间距 |
| xl | 32px | `setSpacing(32)` | 大区块间距 |

#### QLayout 间距设置示例

```cpp
// QVBoxLayout 垂直布局间距
QVBoxLayout* vLayout = new QVBoxLayout();
vLayout->setSpacing(16);  // md 间距
vLayout->setContentsMargins(16, 16, 16, 16);  // 四周边距

// QHBoxLayout 水平布局间距
QHBoxLayout* hLayout = new QHBoxLayout();
hLayout->setSpacing(8);   // sm 间距
hLayout->setContentsMargins(8, 4, 8, 4);

// QGridLayout 网格布局间距
QGridLayout* gridLayout = new QGridLayout();
gridLayout->setSpacing(16);  // md 间距
gridLayout->setHorizontalSpacing(16);
gridLayout->setVerticalSpacing(8);
```

---

### 2.4 圆角规范

| 名称 | 值 | QStyleSheet 属性 | 用途 |
|------|------|------------------|------|
| None | 0px | `border-radius: 0px;` | QLineEdit、分割线 |
| Small | 4px | `border-radius: 4px;` | QPushButton、QLabel 标签 |
| Medium | 8px | `border-radius: 8px;` | QFrame、QGroupBox |
| Large | 12px | `border-radius: 12px;` | QDialog 模态窗口 |

---

## 3. 窗口布局设计

### 3.1 主窗口布局

基于 **QMainWindow** 的标准布局结构：

```
┌─────────────────────────────────────────────────────────────────────┐
│                          标题栏 (40px) - QWidget                    │
├────────────┬────────────────────────────────────────────────────────┤
│            │                    QToolBar (48px)                     │
│            ├────────────────────────────────────────────────────────┤
│            │                                                        │
│   QDockWidget │                                                      │
│   或          │                     QStackedWidget                  │
│   QWidget     │                    (主内容区，自适应)                 │
│   (侧边栏)    │                                                      │
│   200px     │                                                        │
│            │                                                        │
│            ├────────────────────────────────────────────────────────┤
│            │                   QWidget (传输面板, 0-200px)           │
├────────────┴────────────────────────────────────────────────────────┤
│                         QStatusBar (32px)                           │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 主窗口实现

```cpp
class MainWindow : public QMainWindow {
    Q_OBJECT
    
public:
    explicit MainWindow(QWidget* parent = nullptr);
    
private:
    void setupUi();
    void setupMenuBar();      // QMenuBar
    void setupToolBar();      // QToolBar
    void setupSideBar();      // QWidget 或 QDockWidget
    void setupCentralWidget(); // QStackedWidget
    void setupStatusBar();    // QStatusBar
    
private:
    QWidget* m_titleBar;        // 自定义标题栏
    QWidget* m_sideBar;         // 侧边栏
    QStackedWidget* m_contentStack;  // 主内容区堆叠布局
    QWidget* m_transferPanel;   // 传输面板
    QStatusBar* m_statusBar;    // 状态栏
};
```

### 3.3 标题栏设计

**高度**: 40px

**使用 QWidget 自定义标题栏**:

```
┌─────────────────────────────────────────────────────────────────────┐
│ [Logo] Disk                      [最小化] [最大化] [关闭]           │
└─────────────────────────────────────────────────────────────────────┘
```

**QWidget 实现**:

```cpp
class TitleBar : public QWidget {
    Q_OBJECT
    
public:
    explicit TitleBar(QWidget* parent = nullptr);
    
protected:
    void mousePressEvent(QMouseEvent* event) override;
    void mouseMoveEvent(QMouseEvent* event) override;
    
private:
    QLabel* m_logoLabel;
    QLabel* m_titleLabel;
    QPushButton* m_minBtn;
    QPushButton* m_maxBtn;
    QPushButton* m_closeBtn;
};

```

### 3.4 侧边栏设计

**宽度**: 200px（可调整）

**使用 QWidget + QVBoxLayout**:

```
┌──────────────────┐
│                  │
│  🏠 全部文件      │  <- QPushButton / QToolButton
│  ⭐ 收藏夹       │
│  📤 传输列表      │
│  🗑️ 回收站       │
│  🔗 我的分享      │
│                  │
│  ──────────────  │  <- QFrame HLine
│                  │
│  📁 文档         │  <- 快速访问
│  📁 图片         │
│  📁 视频         │
│                  │
└──────────────────┘
```

**QWidget 实现**:

```cpp
class SideBar : public QWidget {
    Q_OBJECT
    
public:
    explicit SideBar(QWidget* parent = nullptr);
    
private:
    void setupNavigationItems();
    void setupQuickAccess();
    
private:
    QVBoxLayout* m_mainLayout;
    QButtonGroup* m_navButtonGroup;  // 导航按钮组
    QFrame* m_separator;              // 分隔线
    QWidget* m_quickAccessWidget;     // 快速访问区域
};


### 3.5 工具栏设计

**高度**: 48px

**使用 QToolBar**:

```
┌─────────────────────────────────────────────────────────────────────┐
│ [⬆ 上传] [📁 新建] │ [◀] [▶] [🔄] │ [🔍 搜索框] │ [⋮ 更多] [⚙] │
└─────────────────────────────────────────────────────────────────────┘
```

**QToolBar 实现**:

```cpp
void MainWindow::setupToolBar() {
    QToolBar* toolbar = addToolBar("MainToolBar");
    toolbar->setMovable(false);
    toolbar->setIconSize(QSize(24, 24));
    toolbar->setFixedHeight(48);
    
    // 主要操作
    QAction* uploadAction = toolbar->addAction(QIcon(":/icons/upload.svg"), tr("上传"));
    QAction* newFolderAction = toolbar->addAction(QIcon(":/icons/folder-new.svg"), tr("新建"));
    
    toolbar->addSeparator();
    
    // 导航控制
    QAction* backAction = toolbar->addAction(QIcon(":/icons/back.svg"), tr("返回"));
    QAction* forwardAction = toolbar->addAction(QIcon(":/icons/forward.svg"), tr("前进"));
    QAction* refreshAction = toolbar->addAction(QIcon(":/icons/refresh.svg"), tr("刷新"));
    
    toolbar->addSeparator();
    
    // 搜索框 - 使用 QLineEdit
    QLineEdit* searchEdit = new QLineEdit();
    searchEdit->setPlaceholderText(tr("搜索文件..."));
    searchEdit->setFixedWidth(250);
    toolbar->addWidget(searchEdit);
    
    // 弹性空间
    QWidget* spacer = new QWidget();
    spacer->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
    toolbar->addWidget(spacer);
    
    // 更多操作
    QAction* moreAction = toolbar->addAction(QIcon(":/icons/more.svg"), tr("更多"));
    QAction* settingsAction = toolbar->addAction(QIcon(":/icons/settings.svg"), tr("设置"));
}


### 3.6 主内容区设计

使用 **QStackedWidget** 管理不同视图页面：

```cpp
// 主内容区布局
void MainWindow::setupCentralWidget() {
    m_contentStack = new QStackedWidget();
    
    // 创建不同视图页面
    QWidget* gridViewPage = createGridViewPage();
    QWidget* listViewPage = createListViewPage();
    QWidget* detailViewPage = createDetailViewPage();
    
    m_contentStack->addWidget(gridViewPage);   // index 0
    m_contentStack->addWidget(listViewPage);   // index 1
    m_contentStack->addWidget(detailViewPage); // index 2
    
    setCentralWidget(m_contentStack);
}
```

#### 3.6.1 网格视图 (QListWidget + IconMode)

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │          │  │          │  │          │  │          │           │
│  │   📁     │  │   📄     │  │   📷     │  │   📁     │           │
│  │          │  │          │  │          │  │          │           │
│  │ 项目文档  │  │ 年度报告  │  │ 团队合影  │  │ 会议记录  │           │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**QListWidget 实现**:

```cpp
QWidget* MainWindow::createGridViewPage() {
    QListWidget* gridView = new QListWidget();
    gridView->setViewMode(QListView::IconMode);
    gridView->setIconSize(QSize(48, 48));
    gridView->setGridSize(QSize(100, 100));
    gridView->setSpacing(16);
    gridView->setResizeMode(QListView::Adjust);
    gridView->setMovement(QListView::Static);
    
    // 添加文件项
    QListWidgetItem* item = new QListWidgetItem(
        QIcon(":/icons/folder.svg"), "项目文档");
    gridView->addItem(item);
    
    return gridView;
}


#### 3.6.2 列表视图 (QTableWidget / QTreeView)

```
┌─────────────────────────────────────────────────────────────────────┐
│  名称                      大小         修改时间         类型       │
├─────────────────────────────────────────────────────────────────────┤
│  📁 项目文档               -           2026-02-15      文件夹       │
│  📄 年度报告.docx          1.2 MB      2026-02-15      文档         │
│  📷 团队合影.jpg           2.3 MB      2026-02-10      图片         │
│  🎬 产品演示.mp4           45.6 MB     2026-02-08      视频         │
└─────────────────────────────────────────────────────────────────────┘
```

**QTableWidget 实现**:

```cpp
QWidget* MainWindow::createListViewPage() {
    QTableWidget* tableView = new QTableWidget();
    tableView->setColumnCount(4);
    tableView->setHorizontalHeaderLabels({
        tr("名称"), tr("大小"), tr("修改时间"), tr("类型")
    });
    tableView->setSelectionBehavior(QAbstractItemView::SelectRows);
    tableView->setSelectionMode(QAbstractItemView::ExtendedSelection);
    tableView->setAlternatingRowColors(true);
    tableView->verticalHeader()->setVisible(false);
    
    // 设置列宽
    tableView->horizontalHeader()->setSectionResizeMode(0, QHeaderView::Stretch);
    tableView->setColumnWidth(1, 100);
    tableView->setColumnWidth(2, 150);
    tableView->setColumnWidth(3, 100);
    
    return tableView;
}


### 3.7 传输面板设计

**高度**: 0-200px（可展开/收起）

**使用 QWidget + QVBoxLayout**:

```
┌─────────────────────────────────────────────────────────────────────┐
│  传输列表 (3)                                           [▼ 收起]    │
├─────────────────────────────────────────────────────────────────────┤
│  📤 上传中                                                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 文档.pdf                    45%     1.2 MB/s     [⏸] [✕]    │   │
│  │ ████████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

**QWidget 实现**:

```cpp
class TransferPanel : public QWidget {
    Q_OBJECT
    
public:
    explicit TransferPanel(QWidget* parent = nullptr);
    void addTransferItem(const TransferInfo& info);
    void setExpanded(bool expanded);
    
private:
    void setupUi();
    
private:
    QVBoxLayout* m_mainLayout;
    QLabel* m_headerLabel;
    QPushButton* m_collapseBtn;
    QScrollArea* m_scrollArea;
    QWidget* m_contentWidget;
    QVBoxLayout* m_contentLayout;
};

// 单个传输项
class TransferItemWidget : public QWidget {
    Q_OBJECT
    
public:
    explicit TransferItemWidget(const TransferInfo& info, QWidget* parent = nullptr);
    void updateProgress(int percent);
    
private:
    QLabel* m_nameLabel;
    QLabel* m_statusLabel;
    QProgressBar* m_progressBar;
    QPushButton* m_pauseBtn;
    QPushButton* m_cancelBtn;
};
```

### 3.8 状态栏设计

**高度**: 32px

**使用 QStatusBar**:

```cpp
void MainWindow::setupStatusBar() {
    QStatusBar* statusBar = this->statusBar();
    statusBar->setFixedHeight(32);
    
    // 左侧：项目数量
    QLabel* itemCountLabel = new QLabel(tr("15 个项目"));
    statusBar->addWidget(itemCountLabel);
    
    // 右侧：存储空间
    QLabel* storageLabel = new QLabel(tr("已用 2.5 GB / 10 GB"));
    statusBar->addPermanentWidget(storageLabel);
}


---

## 4. 组件设计规范

### 4.1 按钮组件 (QPushButton)

#### 4.1.1 主要按钮 (QPushButton)

```
┌──────────────┐
│   上传文件    │  <- QPushButton (Primary)
└──────────────┘
```


#### 4.1.2 次要按钮 (QPushButton)

```
┌──────────────┐
│    取消      │  <- QPushButton (Secondary)
└──────────────┘
```


#### 4.1.3 图标按钮 (QToolButton)

```
┌────┐
│ ⚙  │  <- QToolButton (Icon)
└────┘
```


#### 4.1.4 QPushButton 使用示例

```cpp
// 创建主按钮
QPushButton* uploadBtn = new QPushButton(tr("上传文件"));
uploadBtn->setObjectName("primaryButton");
uploadBtn->setStyleSheet(primaryButtonStyle);

// 创建次按钮
QPushButton* cancelBtn = new QPushButton(tr("取消"));
cancelBtn->setObjectName("secondaryButton");
cancelBtn->setStyleSheet(secondaryButtonStyle);

// 创建图标按钮
QToolButton* settingsBtn = new QToolButton();
settingsBtn->setIcon(QIcon(":/icons/settings.svg"));
settingsBtn->setIconSize(QSize(20, 20));
settingsBtn->setStyleSheet(iconButtonStyle);

// 连接信号槽
connect(uploadBtn, &QPushButton::clicked, this, &MainWindow::onUploadClicked);
connect(cancelBtn, &QPushButton::clicked, this, &QDialog::reject);
```

---

### 4.2 输入框组件

#### 4.2.1 单行输入框 (QLineEdit)

```
┌───────────────────────────────────────┐
│ 搜索文件...                    [🔍]   │
└───────────────────────────────────────┘
```


**QLineEdit 使用示例**:

```cpp
// 创建搜索框
QLineEdit* searchEdit = new QLineEdit();
searchEdit->setPlaceholderText(tr("搜索文件..."));
searchEdit->setClearButtonEnabled(true);
searchEdit->setStyleSheet(lineEditStyle);

// 添加搜索图标
QAction* searchAction = searchEdit->addAction(
    QIcon(":/icons/search.svg"), QLineEdit::TrailingPosition);
connect(searchAction, &QAction::triggered, this, &MainWindow::onSearch);

// 文件名输入
QLineEdit* filenameEdit = new QLineEdit();
filenameEdit->setValidator(new QRegularExpressionValidator(
    QRegularExpression(R"([^\/\\:*?"<>|]+)"), this));
```

#### 4.2.2 多行文本框 (QTextEdit)


---

### 4.3 对话框组件

#### 4.3.1 确认对话框 (QMessageBox)

```
┌─────────────────────────────────────────┐
│  ⚠️ 确认删除                             │
├─────────────────────────────────────────┤
│                                         │
│  确定要删除 "年度报告.docx" 吗？         │
│  此操作将移入回收站，可在 30 天内恢复。  │
│                                         │
├─────────────────────────────────────────┤
│                    [取消]    [确认删除]  │
└─────────────────────────────────────────┘
```

**QMessageBox 实现**:

```cpp
void MainWindow::showDeleteConfirm(const QString& filename) {
    QMessageBox msgBox(this);
    msgBox.setWindowTitle(tr("确认删除"));
    msgBox.setText(tr("确定要删除 \"%1\" 吗？").arg(filename));
    msgBox.setInformativeText(tr("此操作将移入回收站，可在 30 天内恢复。"));
    msgBox.setIcon(QMessageBox::Warning);
    msgBox.setStandardButtons(QMessageBox::Cancel | QMessageBox::Ok);
    msgBox.setDefaultButton(QMessageBox::Cancel);
    msgBox.button(QMessageBox::Ok)->setText(tr("确认删除"));
    
    // 应用样式
    msgBox.exec();
    
    msgBox.exec();
}
```

#### 4.3.2 输入对话框 (QDialog)

```
┌─────────────────────────────────────────┐
│  📝 重命名                               │
├─────────────────────────────────────────┤
│                                         │
│  新名称                                  │
│  ┌───────────────────────────────────┐  │
│  │ 年度报告_v2.docx                  │  │
│  └───────────────────────────────────┘  │
│                                         │
├─────────────────────────────────────────┤
│                    [取消]    [确认]      │
└─────────────────────────────────────────┘
```

**QDialog 实现**:

```cpp
class RenameDialog : public QDialog {
    Q_OBJECT
    
public:
    explicit RenameDialog(const QString& currentName, QWidget* parent = nullptr);
    QString getNewName() const;
    
private:
    void setupUi();
    
private:
    QLineEdit* m_nameEdit;
    QPushButton* m_okBtn;
    QPushButton* m_cancelBtn;
};

// 使用 QVBoxLayout 和 QHBoxLayout 布局
void RenameDialog::setupUi() {
    QVBoxLayout* mainLayout = new QVBoxLayout(this);
    mainLayout->setSpacing(16);
    mainLayout->setContentsMargins(24, 24, 24, 24);
    
    // 标签
    QLabel* label = new QLabel(tr("新名称"));
    mainLayout->addWidget(label);
    
    // 输入框
    m_nameEdit = new QLineEdit();
    m_nameEdit->setText(m_currentName);
    m_nameEdit->selectAll();
    mainLayout->addWidget(m_nameEdit);
    
    // 弹性空间
    mainLayout->addStretch();
    
    // 按钮区域 - QHBoxLayout
    QHBoxLayout* buttonLayout = new QHBoxLayout();
    buttonLayout->addStretch();
    
    m_cancelBtn = new QPushButton(tr("取消"));
    m_okBtn = new QPushButton(tr("确认"));
    m_okBtn->setDefault(true);
    
    buttonLayout->addWidget(m_cancelBtn);
    buttonLayout->addWidget(m_okBtn);
    
    mainLayout->addLayout(buttonLayout);
    
    // 连接信号槽
    connect(m_okBtn, &QPushButton::clicked, this, &QDialog::accept);
    connect(m_cancelBtn, &QPushButton::clicked, this, &QDialog::reject);
    connect(m_nameEdit, &QLineEdit::textChanged, this, &RenameDialog::validateInput);
}
```

---

### 4.4 右键菜单组件 (QMenu)

```
┌──────────────────┐
│ 打开              │
│ 下载              │
├──────────────────┤
│ 重命名            │
│ 移动到...         │
│ 复制到...         │
├──────────────────┤
│ 分享              │
├──────────────────┤
│ 删除              │
└──────────────────┘
```

**QMenu 实现**:

```cpp
void FileListView::showContextMenu(const QPoint& pos) {
    QMenu contextMenu(tr("文件操作"), this);
    
    // 添加菜单项
    QAction* openAction = contextMenu.addAction(QIcon(":/icons/open.svg"), tr("打开"));
    QAction* downloadAction = contextMenu.addAction(QIcon(":/icons/download.svg"), tr("下载"));
    
    contextMenu.addSeparator();
    
    QAction* renameAction = contextMenu.addAction(QIcon(":/icons/rename.svg"), tr("重命名"));
    QAction* moveAction = contextMenu.addAction(QIcon(":/icons/move.svg"), tr("移动到..."));
    QAction* copyAction = contextMenu.addAction(QIcon(":/icons/copy.svg"), tr("复制到..."));
    
    contextMenu.addSeparator();
    
    QAction* shareAction = contextMenu.addAction(QIcon(":/icons/share.svg"), tr("分享"));
    
    contextMenu.addSeparator();
    
    QAction* deleteAction = contextMenu.addAction(QIcon(":/icons/delete.svg"), tr("删除"));
    
    // 连接信号槽
    connect(openAction, &QAction::triggered, this, &FileListView::onOpen);
    connect(downloadAction, &QAction::triggered, this, &FileListView::onDownload);
    // ...
    
    // 显示菜单
    contextMenu.exec(mapToGlobal(pos));
}


---

### 4.5 进度条组件 (QProgressBar)

```
████████████████████░░░░░░░░░░░░░░░░░░  67%
```


**QProgressBar 使用示例**:

```cpp
// 创建进度条
QProgressBar* progressBar = new QProgressBar();
progressBar->setRange(0, 100);
progressBar->setValue(67);
progressBar->setTextVisible(true);  // 或 false 隐藏文字
progressBar->setFormat("%p%");      // 显示百分比
progressBar->setStyleSheet(progressBarStyle);

// 自定义尺寸
progressBar->setFixedHeight(4);
progressBar->setMinimumWidth(200);
```

---

### 4.6 其他常用 Widget 组件

#### 4.6.1 复选框 (QCheckBox)


#### 4.6.2 下拉框 (QComboBox)


---

## 5. 文件图标设计

### 5.1 文件夹图标 (QIcon)

| 状态 | 图标 | QIcon 资源 | 说明 |
|------|------|------------|------|
| 普通 | 📁 | `:/icons/folder.svg` | 默认文件夹 |
| 打开 | 📂 | `:/icons/folder-open.svg` | 当前目录 |
| 收藏 | ⭐ | `:/icons/star.svg` | 收藏夹 |

### 5.2 文件类型图标

| 类型 | 图标 | 扩展名 | QIcon 资源 |
|------|------|--------|------------|
| 文档 | 📄 | doc, docx, txt | `:/icons/file-doc.svg` |
| 表格 | 📊 | xls, xlsx, csv | `:/icons/file-xls.svg` |
| 演示 | 📽️ | ppt, pptx | `:/icons/file-ppt.svg` |
| PDF | 📕 | pdf | `:/icons/file-pdf.svg` |
| 图片 | 🖼️ | jpg, png, gif | `:/icons/file-image.svg` |
| 视频 | 🎬 | mp4, avi, mkv | `:/icons/file-video.svg` |
| 音频 | 🎵 | mp3, wav, flac | `:/icons/file-audio.svg` |
| 压缩包 | 📦 | zip, rar, 7z | `:/icons/file-zip.svg` |
| 代码 | 💻 | cpp, py, js | `:/icons/file-code.svg` |
| 未知 | 📎 | 其他 | `:/icons/file-default.svg` |

### 5.3 图标加载实现

```cpp
// 图标管理器
class IconManager {
public:
    static IconManager& instance();
    
    QIcon folderIcon() const;
    QIcon fileIcon(const QString& extension) const;
    
private:
    IconManager();
    void loadIcons();
    
    QHash<QString, QIcon> m_fileIcons;
    QIcon m_folderIcon;
    QIcon m_defaultIcon;
};

// 使用示例
QIcon icon = IconManager::instance().fileIcon("pdf");
item->setIcon(icon);
```

---

## 6. 动画规范

### 6.1 过渡动画 (QPropertyAnimation)

| 场景 | 时长 | 缓动曲线 | QPropertyAnimation |
|------|------|----------|-------------------|
| 按钮 hover | 150ms | QEasingCurve::OutQuad | - |
| 面板展开 | 200ms | QEasingCurve::InOutQuad | geometry |
| 对话框出现 | 150ms | QEasingCurve::OutQuad | windowOpacity |
| 列表项加载 | 200ms | QEasingCurve::OutQuad | - |
| 页面切换 | 300ms | QEasingCurve::InOutCubic | geometry |

### 6.2 面板展开动画示例

```cpp
void TransferPanel::togglePanel() {
    QPropertyAnimation* animation = new QPropertyAnimation(this, "maximumHeight");
    animation->setDuration(200);
    animation->setEasingCurve(QEasingCurve::InOutQuad);
    
    if (m_expanded) {
        animation->setStartValue(height());
        animation->setEndValue(40);  // 收起状态高度
    } else {
        animation->setStartValue(height());
        animation->setEndValue(200);  // 展开状态高度
    }
    
    m_expanded = !m_expanded;
    animation->start(QAbstractAnimation::DeleteWhenStopped);
}
```

### 6.3 微交互

| 场景 | 实现方式 |
|------|----------|
| 按钮点击 | QStyleSheet `:pressed` 伪状态 |
| 复选框勾选 | QPropertyAnimation 动画 |
| 文件拖入 | QDragEnterEvent + QStyleSheet 边框变化 |
| 上传完成 | QPropertyAnimation + 自定义绘制 |

---

## 7. 响应式设计

### 7.1 断点定义

| 名称 | 宽度 | QLayout 调整 | 说明 |
|------|------|--------------|------|
| Compact | < 800px | 侧边栏使用 QStackedLayout | 侧边栏收起 |
| Medium | 800-1200px | 标准布局 | 侧边栏展开 |
| Expanded | > 1200px | 三栏布局 | 侧边栏可加宽 |

### 7.2 响应式布局实现

```cpp
void MainWindow::resizeEvent(QResizeEvent* event) {
    QMainWindow::resizeEvent(event);
    
    int width = this->width();
    
    if (width < 800) {
        // Compact 模式
        m_sideBar->hide();
        m_sideBarToggleBtn->show();
        m_gridView->setGridSize(QSize(80, 80));
    } else if (width < 1200) {
        // Medium 模式
        m_sideBar->show();
        m_sideBar->setFixedWidth(200);
        m_gridView->setGridSize(QSize(100, 100));
    } else {
        // Expanded 模式
        m_sideBar->show();
        m_sideBar->setFixedWidth(250);
        m_gridView->setGridSize(QSize(120, 120));
    }
}
```

### 7.3 QSizePolicy 使用

```cpp
// 水平扩展的控件
QWidget* spacer = new QWidget();
spacer->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Fixed);

// 固定大小的控件
QPushButton* button = new QPushButton("确定");
button->setSizePolicy(QSizePolicy::Fixed, QSizePolicy::Fixed);

// 最小大小的控件
QLineEdit* searchEdit = new QLineEdit();
searchEdit->setSizePolicy(QSizePolicy::MinimumExpanding, QSizePolicy::Fixed);
searchEdit->setMinimumWidth(200);
```

---

## 8. 国际化

---

## 9. 国际化

### 9.1 支持语言

| 语言 | 代码 | Qt 翻译文件 |
|------|------|-------------|
| 简体中文 | zh_CN | `disk_zh_CN.qm` |
| English | en_US | `disk_en_US.qm` |

### 9.2 Qt 国际化实现

```cpp
// main.cpp
int main(int argc, char* argv[]) {
    QApplication app(argc, argv);
    
    // 加载翻译
    QTranslator translator;
    QString locale = QLocale::system().name();
    if (translator.load(":/translations/disk_" + locale + ".qm")) {
        app.installTranslator(&translator);
    }
    
    MainWindow window;
    window.show();
    
    return app.exec();
}

// 在代码中使用 tr() 函数
QPushButton* button = new QPushButton(tr("上传文件"));
QLabel* label = new QLabel(tr("文件名"));
```

### 9.3 文本布局

- 使用 QWidget 的 sizeHint() 和 minimumSizeHint() 进行自适应
- 使用 QHBoxLayout 和 QVBoxLayout 进行弹性布局
- QLabel 设置 wordWrap 属性支持文本换行

```cpp
QLabel* descriptionLabel = new QLabel();
descriptionLabel->setWordWrap(true);
descriptionLabel->setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
```

---

## 10. 布局管理详解

### 10.1 QVBoxLayout (垂直布局)

```cpp
QVBoxLayout* vLayout = new QVBoxLayout();
vLayout->setSpacing(16);  // 控件间距
vLayout->setContentsMargins(16, 16, 16, 16);  // 边距

// 添加控件
vLayout->addWidget(headerLabel);
vLayout->addWidget(contentWidget, 1);  // stretch = 1
vLayout->addWidget(footerWidget);

// 添加弹性空间
vLayout->addStretch();
```

### 10.2 QHBoxLayout (水平布局)

```cpp
QHBoxLayout* hLayout = new QHBoxLayout();
hLayout->setSpacing(8);

// 添加控件
hLayout->addWidget(iconLabel);
hLayout->addWidget(textLabel, 1);  // stretch = 1

// 添加弹性空间
hLayout->addStretch();

hLayout->addWidget(okButton);
hLayout->addWidget(cancelButton);
```

### 10.3 QGridLayout (网格布局)

```cpp
QGridLayout* gridLayout = new QGridLayout();
gridLayout->setSpacing(16);
gridLayout->setHorizontalSpacing(16);
gridLayout->setVerticalSpacing(8);

// 添加控件到网格
gridLayout->addWidget(nameLabel, 0, 0);
gridLayout->addWidget(nameEdit, 0, 1);
gridLayout->addWidget(sizeLabel, 1, 0);
gridLayout->addWidget(sizeValue, 1, 1);

// 设置列拉伸因子
gridLayout->setColumnStretch(0, 0);  // 标签列不拉伸
gridLayout->setColumnStretch(1, 1);  // 内容列拉伸
```

### 10.4 QStackedLayout (堆叠布局)

```cpp
QStackedLayout* stackedLayout = new QStackedLayout();

// 添加页面
stackedLayout->addWidget(loginPage);     // index 0
stackedLayout->addWidget(mainPage);      // index 1
stackedLayout->addWidget(settingsPage);  // index 2

// 切换页面
stackedLayout->setCurrentIndex(1);
// 或
stackedLayout->setCurrentWidget(mainPage);
```

---

## 附录 A: 设计资源

### 图标资源

- 使用 Material Design Icons (SVG 格式)
- 通过 Qt 资源系统 (.qrc) 管理
- 尺寸：24x24（默认）、48x48（大图标）

### 资源文件示例

```xml
<!-- resources.qrc -->
<RCC>
    <qresource prefix="/icons">
        <file>icons/folder.svg</file>
        <file>icons/file-doc.svg</file>
        <file>icons/upload.svg</file>
        <file>icons/settings.svg</file>
    </qresource>
    <qresource prefix="/translations">
    <qresource prefix="/translations">
        <file>translations/disk_zh_CN.qm</file>
        <file>translations/disk_en_US.qm</file>
    </qresource>
</RCC>
```

### 设计工具

| 工具 | 用途 |
|------|------|
| Qt Designer | 对话框布局预览 |
| Qt Creator | 界面设计集成开发 |
| Figma | UI 设计稿 |
| Inkscape | SVG 图标编辑 |

### Widget 组件速查表

| 组件 | 用途 | QStyleSheet 关键属性 |
|------|------|---------------------|
| QWidget | 基础容器 | background-color, border |
| QPushButton | 按钮 | background-color, border-radius, padding |
| QLineEdit | 单行输入 | border, border-radius, padding |
| QTextEdit | 多行文本 | border, border-radius |
|| QLabel | 文本标签 | color, font-weight |
| QComboBox | 下拉选择 | border, padding, drop-down |
| QCheckBox | 复选框 | indicator, spacing |
| QProgressBar | 进度条 | chunk, text-align |
| QSlider | 滑块 | groove, handle |
| QScrollBar | 滚动条 | handle, add-line, sub-line |
| QMenu | 右键菜单 | item, separator, icon |
| QToolBar | 工具栏 | spacing, QToolButton |
| QStatusBar | 状态栏 | border-top |
| QListWidget | 列表控件 | item, ::item:selected |
| QTableWidget | 表格控件 | item, QHeaderView::section |
| QTreeWidget | 树形控件 | item, branch |

---

*文档版本: v1.0*
*最后更新: 2026-02-19*
*技术栈: Qt 6.x Widgets + QStyleSheet*
