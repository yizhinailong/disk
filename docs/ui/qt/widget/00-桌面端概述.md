# Disk 桌面客户端 (Qt Widgets) - 系统概述

## 1. 项目简介

### 1.1 项目背景

Disk 网盘系统已通过 Drogon 框架实现了高性能的后端服务，提供了完整的文件存储、分享和管理功能。目前已有 TUI（终端用户界面）客户端和 RESTful API，但缺乏适合普通用户的图形化桌面客户端。

本项目旨在为 Disk 网盘系统开发一个现代化、跨平台的 **Qt Widgets 桌面客户端**，为用户提供直观友好的图形化文件管理体验。

### 1.2 项目目标

- **跨平台支持**：支持 Windows、macOS 和 Linux 三大平台
- **原生体验**：采用 Qt Widgets 技术，提供原生的桌面应用体验
- **功能完整**：覆盖后端提供的全部功能
- **高性能**：利用 C++23 特性和 Qt 并发机制，保证响应速度
- **易用性**：符合桌面应用交互习惯，降低学习成本

### 1.3 适用范围

本客户端适用于：
- 需要图形化界面的普通用户
- 企业办公场景的文件管理
- 需要批量上传下载的用户
- 追求稳定性和性能的专业用户
- 对原生桌面体验有要求的用户

---

## 2. 系统架构

### 2.1 总体架构

系统采用经典的 MVC（Model-View-Controller）架构，纯 C++ 实现：

```
┌─────────────────────────────────────────────────────────────────────┐
│                   桌面客户端 (Qt Widgets)                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    表现层 (View/Controller)                   │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │   │
│  │  │  登录窗口    │  │  主窗口     │  │  设置窗口    │          │   │
│  │  │(LoginDialog)│  │(MainWindow) │  │(SettingsDlg)│          │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │   │
│  │  │  文件列表    │  │  传输列表    │  │  分享管理    │          │   │
│  │  │(FileListWgt)│ │(TransferWgt)│ │(SharePanel) │          │   │
│  │  └─────────────┘  └─────────────┘  └─────────────┘          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    业务层 (Service/Controller)                │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │AuthService │  │FileService │  │ShareService│            │   │
│  │  └────────────┘  └────────────┘  └────────────┘            │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │UploadService│ │DownloadSvc │  │TrashService│            │   │
│  │  └────────────┘  └────────────┘  └────────────┘            │   │
│  └─────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │                    基础设施层 (Infrastructure)                 │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │ ApiClient  │  │ TokenStore │  │ ConfigMgr  │            │   │
│  │  └────────────┘  └────────────┘  └────────────┘            │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐            │   │
│  │  │FileWatcher │  │ Notification│ │   Logger   │            │   │
│  │  └────────────┘  └────────────┘  └────────────┘            │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                               │
                               │ HTTPS / RESTful API
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        Disk 服务端 (Drogon)                          │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  • 用户认证 API  • 文件管理 API  • 分享管理 API  • 回收站 API  │   │
│  └─────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 架构特点

| 特性 | 说明 |
|------|------|
| MVC 模式 | View 负责 UI 展示，Controller 处理用户交互，Model 管理数据 |
| 异步非阻塞 | 使用 QNetworkAccessManager 和 Qt Concurrent |
| 信号槽机制 | Qt 信号槽实现组件间通信 |
| 本地缓存 | SQLite 存储离线数据和配置 |
| 系统集成 | 系统托盘、通知、文件关联 |
| 纯代码 UI | 无 .ui 文件，全部使用 C++ 代码创建界面 |

### 2.3 核心设计模式

#### 2.3.1 MVC 架构

```cpp
// Model: 数据模型
class FileModel : public QObject {
    Q_OBJECT
public:
    explicit FileModel(QObject* parent = nullptr);
    
    QList<FileInfo> files() const;
    QString currentPath() const;
    bool isLoading() const;
    
    void refresh();
    void enterFolder(quint64 folderId);
    void navigateUp();

signals:
    void filesChanged();
    void currentPathChanged();
    void loadingChanged();
    void errorOccurred(const QString& message);
    
private:
    QList<FileInfo> m_files;
    QString m_currentPath;
    bool m_loading = false;
};
```

```cpp
// View: 视图层
class FileListView : public QWidget {
    Q_OBJECT
public:
    explicit FileListView(FileModel* model, QWidget* parent = nullptr);
    
private slots:
    void onFilesChanged();
    void onItemDoubleClicked(const QModelIndex& index);
    void onCustomContextMenu(const QPoint& pos);
    
private:
    void setupUi();
    void updateView();
    
    FileModel* m_model;
    QTableWidget* m_tableWidget;
    QStackedWidget* m_stackedWidget;
};
```

```cpp
// Controller: 控制器
class FileController : public QObject {
    Q_OBJECT
public:
    explicit FileController(FileModel* model, QObject* parent = nullptr);
    
public slots:
    void handleRefresh();
    void handleEnterFolder(quint64 folderId);
    void handleNavigateUp();
    void handleDeleteFiles(const QList<quint64>& fileIds);
    void handleRenameFile(quint64 fileId, const QString& newName);
    
private:
    FileModel* m_model;
    FileService* m_fileService;
};
```

#### 2.3.2 服务层模式

```cpp
// 业务服务层
class UploadService : public QObject {
    Q_OBJECT
public:
    explicit UploadService(ApiClient* client, QObject* parent = nullptr);
    
    QFuture<UploadResult> uploadFile(const QString& filePath, quint64 folderId);
    QFuture<UploadResult> resumeUpload(const QString& uploadId);
    void cancelUpload(const QString& uploadId);

signals:
    void progressChanged(const QString& uploadId, qint64 sent, qint64 total);
    void uploadCompleted(const QString& uploadId, const FileInfo& file);
    void uploadFailed(const QString& uploadId, const QString& error);
};
```

#### 2.3.3 信号槽连接模式

```cpp
// View 与 Controller 的连接
class MainWindow : public QMainWindow {
    Q_OBJECT
public:
    explicit MainWindow(QWidget* parent = nullptr);
    
private:
    void setupConnections() {
        // 连接刷新按钮
        connect(m_refreshButton, &QPushButton::clicked,
                m_fileController, &FileController::handleRefresh);
        
        // 连接文件列表双击
        connect(m_fileListView, &FileListView::folderDoubleClicked,
                m_fileController, &FileController::handleEnterFolder);
        
        // 连接模型变化
        connect(m_fileModel, &FileModel::filesChanged,
                m_fileListView, &FileListView::onFilesChanged);
    }
    
    FileModel* m_fileModel;
    FileListView* m_fileListView;
    FileController* m_fileController;
};
```

---

## 3. 技术栈

### 3.1 核心技术

| 组件 | 技术选型 | 版本 | 说明 |
|------|----------|------|------|
| 编程语言 | C++ | C++23 | 现代特性，协程支持 |
| UI 框架 | Qt Widgets | >= 6.5 | 跨平台原生 GUI 框架 |
| 布局管理 | QLayout 系列 | Qt 内置 | QVBoxLayout, QHBoxLayout, QGridLayout |
| 样式系统 | QStyleSheet | Qt 内置 | CSS 类似的样式语法 |
| 网络库 | QNetworkAccessManager | Qt 内置 | HTTP 请求处理 |
| 数据存储 | SQLite | >= 3.35 | 本地数据库 |
| JSON 处理 | QJsonDocument | Qt 内置 | JSON 序列化 |
| 构建系统 | CMake | >= 3.20 | 跨平台构建 |

### 3.2 辅助库

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 日志库 | QLoggingCategory | Qt 内置日志系统 |
| 设置存储 | QSettings | 注册表/INI 配置 |
| 文件监控 | QFileSystemWatcher | 文件变化监听 |
| 多媒体 | Qt Multimedia | 音视频预览 |
| 并发 | Qt Concurrent | 线程池和异步任务 |
| 图标 | Qt Svg / QIcon | 矢量图标支持 |

### 3.3 开发工具

| 工具 | 用途 |
|------|------|
| Qt Creator | IDE（可选，支持 VS Code） |
| CMake | 构建系统 |
| vcpkg | 依赖管理 |
| Qt Test | 单元测试框架 |
| ClangFormat | 代码格式化 |

---

## 4. 功能模块

### 4.1 模块划分

```
┌─────────────────────────────────────────────────────────────────────┐
│                        Disk 桌面客户端                               │
├────────────┬────────────┬────────────┬────────────┬────────────────┤
│  认证模块   │  文件模块   │  传输模块   │  分享模块   │    系统模块     │
├────────────┼────────────┼────────────┼────────────┼────────────────┤
│ • 用户登录  │ • 文件列表  │ • 分片上传  │ • 创建分享  │ • 系统托盘      │
│ • Token管理 │ • 文件预览  │ • 断点续传  │ • 分享列表  │ • 系统通知      │
│ • 自动刷新  │ • 文件下载  │ • 秒传检测  │ • 取消分享  │ • 自动更新      │
│ • 记住密码  │ • 文件操作  │ • 进度显示  │ • 分享访问  │ • 配置管理      │
│            │ • 文件夹管理│ • 传输队列  │            │ • 快捷键        │
│            │ • 文件搜索  │            │            │                │
│            │ • 回收站    │            │            │                │
└────────────┴────────────┴────────────┴────────────┴────────────────┘
```

### 4.2 模块职责

#### 4.2.1 认证模块 (Auth Module)

- **用户登录**：通过用户名/邮箱和密码进行身份验证
- **记住密码**：安全存储登录凭证，支持自动登录
- **Token 管理**：自动刷新 JWT Token，处理过期
- **登出功能**：清除本地凭证，使服务端令牌失效

#### 4.2.2 文件模块 (File Module)

- **文件列表**：QTableWidget 列表视图和自定义网格视图切换
- **文件预览**：支持图片、视频、PDF 等格式预览
- **文件下载**：支持多线程下载和断点续传
- **文件操作**：重命名、移动、复制、删除
- **文件夹管理**：创建、删除、目录树展示（QTreeWidget）
- **文件搜索**：基于文件名的模糊搜索
- **回收站**：查看、恢复、彻底删除

#### 4.2.3 传输模块 (Transfer Module)

- **分片上传**：大文件自动分片上传（5MB 分片）
- **断点续传**：中断后可从断点继续
- **秒传检测**：基于文件哈希的快速上传
- **传输队列**：统一管理上传下载任务
- **系统托盘**：后台传输，最小化到托盘

#### 4.2.4 分享模块 (Share Module)

- **创建分享**：设置有效期和访问密码
- **分享列表**：查看已创建的分享
- **取消分享**：删除分享链接
- **分享访问**：访问他人分享的内容

#### 4.2.5 系统模块 (System Module)

- **系统托盘**：QSystemTrayIcon 后台运行，传输进度显示
- **系统通知**：传输完成、错误通知
- **自动更新**：检测新版本，提示更新
- **配置管理**：服务器地址、下载目录等设置

---

## 5. 界面设计原则

### 5.1 设计风格

| 原则 | 说明 |
|------|------|
| **平台原生感** | 遵循各平台设计规范（Windows/macOS/Linux） |
| **简洁直观** | 信息层次清晰，操作路径简短 |
| **响应式设计** | 窗口大小自适应，支持高分屏 |
| **深色模式** | 支持系统主题跟随，QStyleSheet 动态切换 |
| **一致体验** | 统一的图标、颜色、交互模式 |

### 5.2 主界面布局

```
┌─────────────────────────────────────────────────────────────────────┐
│  [Logo] Disk                           [🔍 搜索...]  [通知] [设置]  │  <- QToolBar / QMenuBar
├─────────────────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌───────────────────────────────────────────────────┐  │
│ │          │ │  📁 根目录 > 文档 > 工作                           │  │  <- QBreadcrumb (自定义)
│ │  快速访问  │ ├───────────────────────────────────────────────────┤  │
│ │  ──────── │ │                                                   │  │
│ │  📁 全部文件│ │   ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐       │  │  <- QTableWidget / 自定义
│ │  ⭐ 收藏   │ │   │ 📁   │  │ 📁   │  │ 📄   │  │ 📷   │       │  │
│ │  🏠 桌面   │ │   │项目   │  │工作   │  │报告   │  │照片   │       │  │
│ │  📥 下载   │ │   └──────┘  └──────┘  └──────┘  └──────┘       │  │
│ │          │ │                                                   │  │
│ │  标签     │ │   ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐       │  │
│ │  ──────── │ │   │ 🎬   │  │ 🎵   │  │ 📦   │  │ 📄   │       │  │
│ │  工作     │ │   │视频   │  │音乐   │  │归档   │  │文档   │       │  │
│ │  生活     │ │   └──────┘  └──────┘  └──────┘  └──────┘       │  │
│ │          │ │                                                   │  │
│ │          │ ├───────────────────────────────────────────────────┤  │
│ │          │ │  📤 上传中 (2) │ 📥 下载中 (1) │ ✓ 已完成 (5)      │  │  <- QTabWidget
│ └──────────┘ └───────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────┤
│  [上传文件] [新建文件夹]                        已用 2.5 GB / 10 GB  │  <- QStatusBar
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 主要 Qt Widgets 组件映射

| 功能区域 | Qt 组件 | 说明 |
|----------|---------|------|
| 主窗口 | QMainWindow | 包含菜单栏、工具栏、状态栏 |
| 侧边栏 | QTreeWidget / QListView | 快速访问、标签导航 |
| 文件列表 | QTableWidget / QListView | 文件展示，支持列表/网格切换 |
| 面包屑 | QWidget (自定义) | 路径导航 |
| 传输面板 | QTabWidget | 上传/下载/已完成标签页 |
| 对话框 | QDialog | 登录、设置、确认等 |
| 系统托盘 | QSystemTrayIcon | 后台运行、通知 |
| 进度条 | QProgressBar | 上传下载进度 |
| 右键菜单 | QMenu | 文件操作上下文菜单 |

---

## 6. 项目结构

```
disk-desktop-widget/
├── CMakeLists.txt                  # 根 CMake 配置
├── vcpkg.json                      # 依赖配置
│
├── src/                            # C++ 源代码
│   ├── main.cpp                    # 程序入口
│   │
│   ├── app/                        # 应用核心
│   │   ├── Application.h/cpp       # QApplication 封装
│   │   └── AppController.h/cpp     # 全局控制器
│   │
│   ├── api/                        # API 客户端
│   │   ├── ApiClient.h/cpp         # HTTP 客户端封装
│   │   ├── AuthApi.h/cpp           # 认证 API
│   │   ├── FileApi.h/cpp           # 文件 API
│   │   ├── FolderApi.h/cpp         # 文件夹 API
│   │   ├── ShareApi.h/cpp          # 分享 API
│   │   └── TrashApi.h/cpp          # 回收站 API
│   │
│   ├── services/                   # 业务服务层
│   │   ├── AuthService.h/cpp       # 认证服务
│   │   ├── FileService.h/cpp       # 文件服务
│   │   ├── UploadService.h/cpp     # 上传服务
│   │   ├── DownloadService.h/cpp   # 下载服务
│   │   └── ShareService.h/cpp      # 分享服务
│   │
│   ├── controllers/                # 控制器层 (MVC - Controller)
│   │   ├── AuthController.h/cpp    # 认证控制器
│   │   ├── FileController.h/cpp    # 文件控制器
│   │   ├── TransferController.h/cpp# 传输控制器
│   │   └── SettingsController.h/cpp# 设置控制器
│   │
│   ├── models/                     # 数据模型层 (MVC - Model)
│   │   ├── FileModel.h/cpp         # 文件模型
│   │   ├── FolderModel.h/cpp       # 文件夹模型
│   │   ├── TransferModel.h/cpp     # 传输任务模型
│   │   └── UserModel.h/cpp         # 用户模型
│   │
│   ├── views/                      # 视图层 (MVC - View)
│   │   ├── MainWindow.h/cpp        # 主窗口
│   │   ├── LoginDialog.h/cpp       # 登录对话框
│   │   ├── SettingsDialog.h/cpp    # 设置对话框
│   │   │
│   │   ├── widgets/                # 自定义控件
│   │   │   ├── FileListView.h/cpp  # 文件列表视图
│   │   │   ├── FileGridView.h/cpp  # 文件网格视图
│   │   │   ├── Breadcrumb.h/cpp    # 面包屑导航
│   │   │   ├── TransferPanel.h/cpp # 传输面板
│   │   │   ├── SidebarWidget.h/cpp # 侧边栏
│   │   │   └── FileInfoPanel.h/cpp # 文件详情面板
│   │   │
│   │   └── dialogs/                # 对话框
│   │       ├── RenameDialog.h/cpp  # 重命名对话框
│   │       ├── DeleteConfirmDialog.h/cpp # 删除确认
│   │       ├── ShareDialog.h/cpp   # 分享对话框
│   │       ├── NewFolderDialog.h/cpp # 新建文件夹
│   │       └── ProgressDialog.h/cpp # 进度对话框
│   │
│   ├── delegates/                  # Qt 委托
│   │   ├── FileItemDelegate.h/cpp  # 文件列表项委托
│   │   └── TransferDelegate.h/cpp  # 传输项委托
│   │
│   ├── storage/                    # 本地存储
│   │   ├── TokenStore.h/cpp        # Token 安全存储
│   │   ├── ConfigStore.h/cpp       # 配置存储
│   │   └── CacheDatabase.h/cpp     # SQLite 缓存
│   │
│   └── utils/                      # 工具类
│       ├── FileUtils.h/cpp         # 文件工具
│       ├── HashCalculator.h/cpp    # 哈希计算
│       ├── FormatUtils.h/cpp       # 格式化工具
│       ├── StyleHelper.h/cpp       # 样式辅助
│       └── Logger.h/cpp            # 日志封装
│
├── resources/                      # 资源文件
│   ├── resources.qrc               # Qt 资源文件
│   ├── icons/                      # 图标资源
│   │   ├── light/                  # 浅色主题图标
│   │   └── dark/                   # 深色主题图标
│   ├── images/                     # 图片资源
│   ├── styles/                     # QSS 样式表
│   │   ├── light.qss               # 浅色主题
│   │   └── dark.qss                # 深色主题
│   └── translations/               # 国际化文件
│       ├── app_zh_CN.ts            # 中文翻译
│       └── app_en_US.ts            # 英文翻译
│
├── tests/                          # 测试代码
│   ├── unit/                       # 单元测试
│   │   ├── models/                 # 模型测试
│   │   ├── services/               # 服务测试
│   │   └── utils/                  # 工具类测试
│   └── integration/                # 集成测试
│
├── deploy/                         # 部署配置
│   ├── windows/                    # Windows 部署
│   ├── macos/                      # macOS 部署
│   └── linux/                      # Linux 部署
│
└── docs/                           # 文档
    ├── README.md                   # 项目说明
    └── BUILD.md                    # 构建指南
```

---

## 7. 性能指标

### 7.1 目标指标

| 指标类型 | 指标项 | 目标值 |
|----------|--------|--------|
| 启动时间 | 冷启动 | < 2 秒 |
| 启动时间 | 热启动（已登录） | < 1 秒 |
| 响应时间 | UI 操作响应 | < 100ms |
| 响应时间 | 文件列表加载（100项） | < 500ms |
| 内存占用 | 空闲状态 | < 100 MB |
| 内存占用 | 大列表渲染（10000项） | < 300 MB |
| 传输速度 | 上传速度 | 达到网络带宽上限 |
| 传输速度 | 下载速度 | 达到网络带宽上限 |

### 7.2 优化策略

#### 7.2.1 渲染优化

1. **虚拟列表**：大列表使用 QTableView + QAbstractItemModel 的懒加载
2. **图片缓存**：缩略图使用 QPixmapCache 缓存
3. **延迟加载**：非关键组件延迟初始化
4. **双缓冲**：自定义控件使用双缓冲绘制

#### 7.2.2 内存优化

1. **按需加载**：文件列表分页加载
2. **对象池**：复用 QWidget 和 QListWidgetItem
3. **智能缓存**：LRU 缓存策略
4. **延迟删除**：使用 deleteLater() 安全释放

#### 7.2.3 网络优化

1. **连接复用**：HTTP Keep-Alive
2. **并发控制**：合理控制并发请求数
3. **断点续传**：大文件支持断点续传
4. **压缩传输**：启用 gzip 压缩

---

## 8. 版本规划

| 版本 | 功能范围 | 状态 |
|------|----------|------|
| v0.1 | 项目骨架、基础配置、登录界面 | 计划中 |
| v0.2 | 文件列表（表格/网格视图）、文件下载、文件夹导航 | 计划中 |
| v0.3 | 文件上传（基础）、文件操作（重命名/删除） | 计划中 |
| v0.4 | 分片上传、断点续传、秒传、传输队列 | 计划中 |
| v0.5 | 文件预览（图片、PDF）、分享功能 | 计划中 |
| v0.6 | 回收站功能、文件搜索 | 计划中 |
| v0.7 | 系统托盘、系统通知、深色模式 | 计划中 |
| v0.8 | 自动更新、国际化支持 | 计划中 |
| v0.9 | 性能优化、Bug 修复 | 计划中 |
| v1.0 | 完整功能、全面测试、文档完善 | 计划中 |

---

## 9. 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| QWidget | QWidget | Qt 所有控件的基类 |
| MVC | Model-View-Controller | 一种 UI 架构模式 |
| Signal/Slot | Signal/Slot | Qt 的信号槽机制 |
| QLayout | QLayout | Qt 布局管理器基类 |
| QStyleSheet | QStyleSheet | Qt 样式表，类似 CSS |
| QTableWidget | QTableWidget | Qt 表格控件 |
| QTreeWidget | QTreeWidget | Qt 树形控件 |
| QListView | QListView | Qt 列表视图 |
| QSystemTrayIcon | QSystemTrayIcon | Qt 系统托盘图标 |
| 分片上传 | Chunked Upload | 将大文件分割成小块逐个上传 |
| 断点续传 | Resume Upload | 中断后从上次位置继续上传 |
| 秒传 | Instant Upload | 通过文件哈希匹配已存在的文件 |
| 委托 | Delegate | Qt 中用于自定义项渲染和编辑的类 |

---

## 10. 参考资料

### 10.1 官方文档

- [Qt 6 官方文档](https://doc.qt.io/qt-6/)
- [Qt Widgets 官方文档](https://doc.qt.io/qt-6/qtwidgets-index.html)
- [Qt 样式表参考](https://doc.qt.io/qt-6/stylesheet-reference.html)
- [Qt 布局管理](https://doc.qt.io/qt-6/layout.html)

### 10.2 相关文档

- [Disk 系统概述](../../../docs/00-系统概述.md)
- [Disk 功能需求规格](../../../docs/01-功能需求规格.md)
- [Disk API 接口设计](../../../docs/02-API接口设计.md)
- [TUI 客户端文档](../tui/00-TUI概述.md)

### 10.3 代码风格说明

本客户端遵循 Qt 惯例，使用 **camelCase** 命名风格：

| 类型 | 命名风格 | 示例 |
|------|----------|------|
| 类名 | PascalCase | `MainWindow`, `FileController` |
| 方法名 | camelCase | `refreshFiles()`, `handleUpload()` |
| 变量名 | camelCase | `fileName`, `currentPath` |
| 成员变量 | m_ + camelCase | `m_fileModel`, `m_apiClient` |
| 常量 | UPPER_SNAKE_CASE | `MAX_FILE_SIZE`, `DEFAULT_TIMEOUT` |
| 信号 | camelCase + Changed | `filesChanged()`, `loadingChanged()` |
| 槽函数 | on + PascalCase | `onRefreshClicked()`, `onFileSelected()` |

> **注意**：与后端 C++ 代码（Drogon）的 PascalCase 方法命名不同，Qt Widgets 客户端遵循 Qt 社区的 camelCase 惯例。

---

*文档版本: v1.0*
*最后更新: 2026-02-19*
